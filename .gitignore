# Project-level sensitive / private files
instructions.md

# Root-level Docker Compose env — contains real credentials, never commit this
/.env
# .env.example is committed intentionally as a safe template

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Frontend (to be added later)
frontend/node_modules/
frontend/dist/
frontend/.env.local

# Backend — Symfony handles its own .gitignore inside backend/
async-email-notifications.md
