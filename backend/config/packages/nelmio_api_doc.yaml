nelmio_api_doc:
    documentation:
        info:
            title: Workout Plan Manager API
            description: REST API for managing workout plans, exercises and user assignments.
            version: 1.0.0
        servers:
            - url: http://localhost:8080
              description: Local Docker environment
    areas:
        path_patterns:
            - ^/api(?!/doc) # All /api routes except /api/doc itself
