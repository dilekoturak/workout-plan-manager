# Docker Compose environment variables
# Copy this file to .env and fill in your actual values.
# This file is committed — do NOT put real credentials here.

POSTGRES_DB=workout_plan_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# Mailer DSN — Mailtrap sandbox for local/dev testing
MAILER_DSN=smtp://798f0ad5afdae7:549ecec3b71614@sandbox.smtp.mailtrap.io:2525

# Mailer DSN — use Mailpit for local dev (see docker-compose.yml)
# For production use SMTP: smtp://user:pass@smtp.example.com:587
MAILER_DSN=smtp://mailpit:1025
